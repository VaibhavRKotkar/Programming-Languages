#include<stdio.h>

int main()
{
    int a= 5, b = 4, c = 0;
    c= a+2*b;
    printf("the answer is:%d\n", c);
   // multiplication first then other.
   // BODMAS rule is applied here.
return 0;
}